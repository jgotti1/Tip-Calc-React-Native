# Tip Calc

A React Native (Expo) tip calculator for iOS.

## Portfolio card

The block below is machine-readable project info for a portfolio site (invisible on GitHub). Keep it in sync when the project, URL or tech changes. To build a card: read this JSON and use `title`, `tagline`/`description`, `thumbnail`, `tech`, and link to `liveUrl` and `repoUrl`. `liveUrl` is null on purpose: the app is not deployed to the web; it is distributed through the Apple App Store, so show an "Available on the Apple App Store" badge instead of a live link.

<!-- portfolio-card:start
{
  "title": "Tip Calc",
  "category": "mobile app",
  "tagline": "A simple mobile tip calculator with bill splitting.",
  "description": "Tip Calc is a React Native app built with Expo for working out tips at a glance. Enter the bill amount, pick a tip percentage, choose how many people are splitting it, and see the tip and total, with an optional round-up. It is available on the Apple App Store and is not deployed as a website, so the card shows a preview image instead of a live link.",
  "liveUrl": null,
  "availability": "Available on the Apple App Store (no public web link)",
  "repoUrl": "https://github.com/jgotti1/Tip-Calc-React-Native",
  "thumbnail": "https://raw.githubusercontent.com/jgotti1/Tip-Calc-React-Native/main/docs/preview.jpg",
  "tech": ["React Native", "Expo", "JavaScript"],
  "features": ["Bill amount entry", "Split the bill between people", "Tip percentage picker with service ratings", "Tip amount and total shown separately", "Round-up option", "Reset values button"],
  "platforms": ["mobile"],
  "status": "live",
  "origin": "Personal project"
}
portfolio-card:end -->
